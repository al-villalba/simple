{
	"/{namespace}/{controller}/{action}/*" : {
	},

	"/{namespace}/{controller}/{action}" : {
	},

	"/force/{action}/*" : {
		"controller" : "ForceController",
		"query"     : {
			"p1" : "v1",
			"p2" : "v2"
		}
	},

	"/{controller}/{action}/*" : {
	},

	"/{controller}/{action}" : {
	},

	"/{controller}" : {
		"action" : "index"
	},

	"/" : {
		"controller" : "Homepage",
		"action"     : "index"
	}
}
